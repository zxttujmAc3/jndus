apiVersion: v1
kind: Service
metadata:
  name: sjdufh
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "sjdufh"
    port: 8080
  selector:
    app: sjdufh
